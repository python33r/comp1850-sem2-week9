# Linked-list

## List data structure

    List: { Node *head, int size }

## List operations

`addNode()` and `removeNode()`

## Tasks

### 1. Inspect the `Makefile`
- Compile with `make`

### 2. Inspect the code
- inspect the implementation of the list functions
    - check the modifications to the list in the `addNode()` and `removeNode()` function
    - can you match them to the diagram on the slides?
- run the code and inspect the output

### 3. Test with `valgrind`
- do we have memory leaks?
- if so, locate and fix them
- Hint: you can also compare to session 1 tasks, eg. the stack implementation

### 4. Implementation
- add code to `main()` to test `addNode()` and `removeNode()`
- display the list data using 'traverseI()` to verify the behaviour
- sketch the structure and modifications to predict the output 
